{
  "name": "academic-cv-generator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:site": "eleventy",
    "build:pdf": "node scripts/render-pdf.mjs",
    "build": "npm run build:site && npm run build:pdf",
    "dev": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "gray-matter": "^4.0.3",
    "markdown-it": "^14.1.0",
    "playwright": "^1.53.0"
  }
}
